"""
Models for AI engine.
No additional models needed - uses existing Expense model.
"""
from django.db import models

# AI engine doesn't require additional database models
# It uses the existing Expense model from expenses app
